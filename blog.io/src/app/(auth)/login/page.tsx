import LoginPage from '@/components/forms/loging'
import React, { Suspense } from 'react'

const Login = () => {
  return (
    <Suspense>
      <LoginPage />
    </Suspense>
  )
}

export default Login

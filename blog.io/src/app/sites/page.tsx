import SitesPage from '@/components/sites/page'
import React, { Suspense } from 'react'

const Sites = () => {
  return (
    <Suspense>
      <SitesPage />
    </Suspense>
  )
}

export default Sites
